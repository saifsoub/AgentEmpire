# GitHub-native Secret Rotator schedule example.
# Keep real provider names and secret ids here; keep credentials in GitHub Actions secrets.
secrets:
  - id: "prod-db-password"
    provider: "aws"
    interval: "24h"
  - id: "stripe-key"
    provider: "vault"
    interval: "7d"
